rootProject.name = "lets-plot-demo"

pluginManagement {
    plugins {
        kotlin("multiplatform") version "2.1.20-Beta1" apply false
        id("org.jetbrains.dokka") version "2.0.0" apply false
    }
}
